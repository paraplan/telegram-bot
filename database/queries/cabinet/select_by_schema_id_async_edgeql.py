# AUTOGENERATED FROM 'database/queries/cabinet/select_by_schema_id.edgeql' WITH:
#     $ edgedb-py --dir database/queries --no-skip-pydantic-validation


from __future__ import annotations

import dataclasses
import uuid

import edgedb


@dataclasses.dataclass
class SelectBySchemaIdResult:
    id: uuid.UUID
    number: str
    schema_id: int
    title: str


async def select_by_schema_id(
    executor: edgedb.AsyncIOExecutor,
    *,
    schema_id: int,
) -> list[SelectBySchemaIdResult]:
    return await executor.query(
        """\
        select (Cabinet) {*} filter { .schema_id = <int16>$schema_id };\
        """,
        schema_id=schema_id,
    )
